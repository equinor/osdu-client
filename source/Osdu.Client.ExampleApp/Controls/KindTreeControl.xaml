<UserControl x:Class="Osdu.Client.ExampleApp.Controls.KindTreeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="280">
    <UserControl.Resources>
        <QuadraticEase x:Key="EaseOut" EasingMode="EaseOut"/>

        <!-- Chevron arrow toggle button for expander -->
        <Style x:Key="ExpanderChevronToggle" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ToggleBorder"
                                Background="Transparent"
                                CornerRadius="3"
                                Padding="4,6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Chevron icon -->
                                <Path x:Name="Arrow"
                                      Grid.Column="0"
                                      Data="M 0,0 L 5,4 L 0,8"
                                      Stroke="{DynamicResource ExpanderArrowBrush}"
                                      StrokeThickness="1.5"
                                      Fill="Transparent"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      Width="10" Height="10"
                                      Stretch="Uniform"
                                      RenderTransformOrigin="0.5,0.5"
                                      SnapsToDevicePixels="True">
                                    <Path.RenderTransform>
                                        <RotateTransform x:Name="ArrowRotation" Angle="0"/>
                                    </Path.RenderTransform>
                                </Path>
                                <!-- Folder icon in its own column -->
                                <Path x:Name="FolderIcon"
                                      Grid.Column="1"
                                      Margin="6,0,6,0"
                                      Width="14" Height="12"
                                      Stretch="Uniform"
                                      VerticalAlignment="Center"
                                      Data="M 1,3 L 5,3 L 7,1 L 13,1 L 13,11 L 1,11 Z"
                                      Stroke="{DynamicResource AccentBrush}"
                                      StrokeThickness="1"
                                      Fill="{DynamicResource FolderFillBrush}"/>
                                <!-- Header text -->
                                <ContentPresenter Grid.Column="2"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ArrowRotation"
                                                             Storyboard.TargetProperty="Angle"
                                                             To="90" Duration="0:0:0.15"
                                                             EasingFunction="{StaticResource EaseOut}"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ArrowRotation"
                                                             Storyboard.TargetProperty="Angle"
                                                             To="0" Duration="0:0:0.15"
                                                             EasingFunction="{StaticResource EaseOut}"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Arrow" Property="Stroke" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="ToggleBorder" Property="Background" Value="{DynamicResource CardHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Expander style -->
        <Style x:Key="KindExpanderStyle" TargetType="Expander">
            <Setter Property="Margin" Value="0,1,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <DockPanel>
                            <ToggleButton x:Name="HeaderToggle"
                                          DockPanel.Dock="Top"
                                          Style="{StaticResource ExpanderChevronToggle}"
                                          Content="{TemplateBinding Header}"
                                          IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Cursor="Hand"/>
                            <ContentPresenter x:Name="ExpandContent"
                                              Visibility="Collapsed"
                                              Margin="16,0,0,0"/>
                        </DockPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandContent" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBox style: virtualized, NO internal ScrollViewer so outer one handles scrolling -->
        <Style x:Key="KindListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <ItemsPresenter/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemsPanel">
                <Setter.Value>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem style with hover -->
        <Style x:Key="KindItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="6,3,6,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                Margin="0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Document icon -->
                                <Path Grid.Column="0"
                                      Width="11" Height="13"
                                      Stretch="Uniform"
                                      VerticalAlignment="Center"
                                      Data="M 2,0 L 8,0 L 12,4 L 12,14 L 2,14 Z M 8,0 L 8,4 L 12,4"
                                      Stroke="{DynamicResource TextMutedBrush}"
                                      StrokeThickness="0.8"
                                      Fill="Transparent"/>
                                <ContentPresenter Grid.Column="1"
                                                  Margin="8,0,0,0"
                                                  VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background"
                                        Value="{DynamicResource CardHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background"
                                        Value="{DynamicResource SelectionBgBrush}"/>
                                <Setter Property="Foreground"
                                        Value="{DynamicResource SelectionTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search box style -->
        <Style x:Key="FilterBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="6,6,6,6"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- Search icon (magnifying glass) -->
                                <Path Grid.Column="0"
                                      Width="13" Height="13"
                                      Stretch="Uniform"
                                      Margin="0,0,6,0"
                                      VerticalAlignment="Center"
                                      Data="M 9,0 A 5,5 0 1 0 9,10 A 5,5 0 1 0 9,0 M 4.5,9.5 L 0,14"
                                      Stroke="{DynamicResource TextMutedBrush}"
                                      StrokeThickness="1.5"
                                      Fill="Transparent"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="Placeholder"
                                           Grid.Column="1"
                                           Text="Filter kinds..."
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <DockPanel>
        <StackPanel x:Name="ToolbarPanel" DockPanel.Dock="Top"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="8,4,8,0">
            <Button x:Name="ExpandAllButton" Click="ExpandAllButton_Click"
                    ToolTip="Expand All" Cursor="Hand"
                    Background="Transparent" BorderThickness="0" Padding="4,2">
                <Path Width="14" Height="14" Stretch="Uniform"
                      Data="M 0,2 L 7,9 L 14,2 M 0,8 L 7,15 L 14,8"
                      Stroke="{DynamicResource TextSecondaryBrush}"
                      StrokeThickness="1.5" Fill="Transparent"/>
            </Button>
            <Button x:Name="CollapseAllButton" Click="CollapseAllButton_Click"
                    ToolTip="Collapse All" Cursor="Hand"
                    Background="Transparent" BorderThickness="0" Padding="4,2" Margin="4,0,0,0">
                <Path Width="14" Height="14" Stretch="Uniform"
                      Data="M 0,9 L 7,2 L 14,9 M 0,15 L 7,8 L 14,15"
                      Stroke="{DynamicResource TextSecondaryBrush}"
                      StrokeThickness="1.5" Fill="Transparent"/>
            </Button>
        </StackPanel>
        <TextBox x:Name="FilterBox"
                 DockPanel.Dock="Top"
                 Style="{StaticResource FilterBoxStyle}"
                 TextChanged="FilterBox_TextChanged"/>
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="KindPanel" Margin="4,0"/>
        </ScrollViewer>
    </DockPanel>
</UserControl>